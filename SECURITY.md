# Security Policy

Newest Files reads vault file metadata through Obsidian public APIs, stores a plugin-managed first-seen index in its own plugin data, and opens files through the workspace API. It does not write vault files, use network APIs, access the clipboard, patch native Obsidian internals, or copy Obsidian source.

Only the latest GitHub release is actively supported.
